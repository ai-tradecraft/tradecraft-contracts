[project]
name = "tradecraft-contracts"
version = "0.1.0"
description = "Shared Tradecraft JSON Schema and protocol contracts."
requires-python = ">=3.12"
dependencies = [
  "jsonschema",
]

[tool.uv]
package = false
